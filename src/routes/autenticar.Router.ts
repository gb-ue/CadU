import express from "express";
const autenticarRouter = express.Router();

autenticarRouter.post( '/login', (req,res) => {
} )

autenticarRouter.post( '/cadastro', (req,res) => {
} )

export default autenticarRouter;
